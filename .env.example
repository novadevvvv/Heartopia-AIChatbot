# Copy this file to `.env` and fill in your values.

# Required for Groq calls
heartopiaChatAPI=your_groq_api_key_here

# Optional: set to 1 to run Groq chat integration tests
RUN_CHAT_TESTS=0

# Optional: set to 1 to run screenshot integration tests
RUN_VISION_TESTS=0

# Optional: directory path to save cropped chat images during vision tests/debugging
# HEARTOPIA_DEBUG_CROPS_DIR=debug_crops
